# Backend-for-Frontend Examples

The BFF is the security boundary for UAE PASS. It owns authorization initiation,
state, PKCE, callback handling, token exchange, user-info retrieval, session rotation,
and logout.

- [Node.js/Express BFF](nodejs-express/README.md)

The repository intentionally contains no token-mediating proxy example.
